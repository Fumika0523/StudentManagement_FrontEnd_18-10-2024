import SampleChart from "./SampleChart";

// ========================================
// SAMPLE CHART WRAPPER
// ========================================

/*
 * No props are required here, so TypeScript
 * does not need a props interface.
 */
const ChartDisplay = () => {
  return (
    <SampleChart />
  );
};

export default ChartDisplay;