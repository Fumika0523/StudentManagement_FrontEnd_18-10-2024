// ========================================
// STUDENT ATTENDANCE PAGE
// ========================================

/*
 * This component currently has no props,
 * state, or API calls, so no additional
 * TypeScript interfaces are required yet.
 */
const Attendance = () => {
  return (
    <div>
      Attendance
    </div>
  );
};

export default Attendance;