// ========================================
// STUDENT RESULT PAGE
// ========================================

/*
 * This component currently has no props,
 * state, or API calls, so no additional
 * TypeScript types are required yet.
 */
const Result = () => {
  return (
    <div>
      Result
    </div>
  );
};

export default Result;